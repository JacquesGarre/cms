<form method="post" action="{{ path('app_front_office_page_delete', {'id': front_office_page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ front_office_page.id) }}">
    <button class="btn">Delete</button>
</form>
