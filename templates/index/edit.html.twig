{% extends 'base.html.twig' %}

{% block title %}Edit Index{% endblock %}

{% block body %}
    <h1>Edit {{index.name}}</h1>

    {{ include('index/_form.html.twig', {'button_label': 'Update'}) }}
    <a href="{{ path('app_form_edit', {'id':model.id} ) }}">back to list</a>
    {{ include('index/_delete_form.html.twig') }}

    <hr>
    {{ include('index_column/index.html.twig') }}

{% endblock %}
