{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}

{% block body %}
    <h1>Index</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ index.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ index.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_index_index') }}">back to list</a>

    <a href="{{ path('app_index_edit', {'id': index.id}) }}">edit</a>

    {{ include('index/_delete_form.html.twig') }}
{% endblock %}
