<form method="post" action="{{ path('app_textarea_delete', {'id': textarea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ textarea.id) }}">
    <button class="btn">Delete</button>
</form>
